@model IEnumerable<UDG.Colloquium.BL.Entities.Account.RoleNamesDTO>

@{
    ViewBag.Title = "ManageRoles";
}

<h2>ManageRoles</h2>

<p>
    @Html.ActionLink("Create New Role", "CreateRole")
</p>
@PageHelpers.Modal("Role Deletion Confirmation","Are you sure you want to delete the following role:")
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoleName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RoleName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id,roleName=item.RoleName }) |
            @Html.ActionLink("Delete", "DeleteRole", new { id = item.Id, roleName = item.RoleName }, new { id=""+@item.RoleName+"",@class="modalActionLink" })
        </td>
    </tr>
}
</table>

@section scripts{
    @Scripts.Render("~/bundles/custom/colloquiumCore")
}
